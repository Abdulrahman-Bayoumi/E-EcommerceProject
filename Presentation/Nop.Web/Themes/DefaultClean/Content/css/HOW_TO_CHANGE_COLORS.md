# 🎨 كيفية تغيير الألوان في nopCommerce

## 📍 الملف الرئيسي لتغيير الألوان:

**الملف:** `Presentation/Nop.Web/Themes/DefaultClean/Content/css/styles.css`

## 🎯 الألوان الأساسية (في بداية الملف):

افتح الملف وستجد في السطور 2-9:

```css
:root {
  --accent-blue-color: #4ab2f1;        /* اللون الأزرق الأساسي (الأزرار، الروابط) */
  --accent-blue-active-color: #248ece; /* اللون الأزرق عند الضغط */
  --gray-color: #6B6B6B;               /* اللون الرمادي للنصوص */
  --dark-gray-color: #444;              /* اللون الرمادي الداكن للعناوين */
  --error-red-color: #d51f28;          /* لون الأخطاء */
  --success-green-color: #207d4c;       /* لون النجاح */
}
```

## 🔧 كيفية التغيير:

### 1. تغيير اللون الأزرق الأساسي:
غيّر `--accent-blue-color` من `#4ab2f1` إلى أي لون تريده، مثلاً:
- `#00d4ff` (أزرق فاتح)
- `#6366f1` (بنفسجي أزرق)
- `#10b981` (أخضر)
- `#f59e0b` (برتقالي)

### 2. تغيير لون الأزرار عند الضغط:
غيّر `--accent-blue-active-color` إلى لون أغمق قليلاً من اللون الأساسي

### 3. تغيير ألوان النصوص:
- `--gray-color`: للنصوص العادية
- `--dark-gray-color`: للعناوين

## 📝 أمثلة:

### مثال 1: تغيير إلى أزرق فاتح
```css
:root {
  --accent-blue-color: #00d4ff;
  --accent-blue-active-color: #0099cc;
}
```

### مثال 2: تغيير إلى أخضر
```css
:root {
  --accent-blue-color: #10b981;
  --accent-blue-active-color: #059669;
}
```

### مثال 3: تغيير إلى برتقالي
```css
:root {
  --accent-blue-color: #f59e0b;
  --accent-blue-active-color: #d97706;
}
```

## ⚠️ ملاحظات مهمة:

1. **احفظ الملف** بعد التعديل
2. **أعد بناء المشروع**: `dotnet build`
3. **شغّل التطبيق**: `dotnet run`
4. **امسح الـ Cache**: اضغط `Ctrl+Shift+R` في المتصفح

## 🔍 أماكن أخرى للألوان:

إذا أردت تغيير ألوان محددة أكثر، ابحث في الملف عن:
- `.button-1` - أزرار رئيسية
- `.header` - الهيدر
- `.footer` - الفوتر
- `.product-item` - بطاقات المنتجات

## 💡 نصيحة:

استخدم [Color Picker](https://htmlcolorcodes.com/color-picker/) لاختيار الألوان المناسبة!

